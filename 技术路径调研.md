# DNA Retrieval Engine 技术路径调研

当前版本：v2.0

> 调研日期：2026-08-27  
> 项目性质：同济大学数据结构课程设计，Windows 本地桌面演示程序  
> 依据：`课程设计.txt`、`prd/` 原型、当前仓库状态、`D:\工具开发\pywebview桌面应用与EXE打包实践.md` 及文末官方资料

## 0. 2026-08-28 问题修复记录

- 生成器将错配范围收敛到 0-3，并按 Read 随机抽样且保证每类至少一条；真值、检索参数、报告分布和前端滑块共用同一上限。
- 匹配器在提前剪枝时保留 0-3 范围内的最佳被拒绝候选 `nearest_hit`，坐标图使用红色条带，对比沙盘和 Reads 列表使用“错配 x”红色状态。
- 文件名按钮增加固定宽度与省略号；右侧 Inspector 支持收起/重新打开；索引桶表使用固定布局和断词规则，避免内容漂移出卡片。
- 生成、加载、重建索引、检索、导出均由 `TaskController` 后台线程执行。桥接未就绪、任务运行中或缺少前置数据时，前端和 API 双重锁定/拒绝按钮，`get_state()` 轮询继续作为事件推送失败的兜底。
- 新增非 Playwright 的 pytest 回归用例，覆盖随机分布、阈值边界、`nearest_hit`、任务立即返回、非法调用拒绝和前端接线静态检查。
- 根据 pywebview 官方文档复核：`file://` 对 JS Bridge 存在限制，生产实现迁移到 pywebview 内置 HTTP Server；后台线程不再直接 `evaluate_js`，改由前端单飞轮询作为权威状态通道。
- 前端权限重构为“本地 UI 与后端业务分离”：帮助、用户/项目/视图菜单、参数和输入方式切换不依赖数据；索引、Reads、报告页的业务按钮在空数据状态由 HTML 初始属性和统一 capability 矩阵双重锁定。
- 内存根因最终确认为公开 `js_api` 内部对象递归暴露和顶层多文档导航累积。内部服务全部私有化，四页改成单文档视图路由；160 次压力切换后内存回落，详见 `docs/stability-investigation.md`。
- 加入真实启动遮罩握手和 `notify_frontend_ready` 探针；打包版 12 次冷启动全部完成 HTTP、Bridge、四视图和首次状态同步。

## 1. 结论先行

本项目适合采用“纯 Python 算法核心 + pywebview 桌面外壳 + 原生 HTML/CSS/JavaScript 前端 + PyInstaller 单文件打包”的路线。无需引入 Flask、FastAPI、React、数据库、NumPy、Pandas 或 Biopython；这些依赖会增加打包体积和答辩解释成本，也不能替代课程要求的手写核心数据结构。

推荐基线如下：

| 项目 | 建议 |
|---|---|
| 目标系统 | Windows 10/11 x64 |
| Python | `3.13.15` x64，开发、测试和打包统一版本 |
| 桌面容器 | `pywebview==6.2.1` |
| Windows 桥接依赖 | `pythonnet==3.1.0`，由锁文件固定 |
| Windows 渲染器 | Edge Chromium / WebView2 |
| 打包 | `PyInstaller==6.22.2`，先 `onedir` 调试，再生成 `onefile` |
| 核心算法 | 纯 Python，手写拉链法哈希表、桶链表和位置单链表 |
| 数据格式 | 参考序列用 FASTA，Reads 用 FASTQ，生成真值用 JSON |
| 测试 | `pytest`，核心结构、算法、生成器、文件解析、桥接和打包冒烟测试 |
| 前端 | 复用 `prd/` 的视觉和布局，生产副本放入 Python 包内；移除所有硬编码结果和 CDN 依赖 |

本项目已经在当前 `.venv` 的 CPython 3.13.15 x64 上完成精确版本验证：安装 `pywebview==6.2.1`、`pythonnet==3.1.0` 和 `PyInstaller==6.22.2` 后，`pip check` 无依赖冲突；强制 `edgechromium` 能创建 `Microsoft.Web.WebView2.WinForms.WebView2` 原生控件，DOM 读取和 `window.pywebview.api` 注入均正常；现有 `prd/index.html` 也已通过真实隐藏窗口加载冒烟。因此不再保留“优先使用 Python 3.12”的旧建议，正式基线统一为 Python 3.13.15。最终 EXE 仍必须在干净 Windows 虚拟机做完整流程验收，因为解释器/桥接实测通过不等于所有静态资源已经正确打包。

需要提前接受一个部署边界：pywebview 在 Windows 上优先使用 WebView2。Windows 11 和绝大多数 Windows 10 设备已有 Evergreen WebView2 Runtime，但不是所有 Windows 10 机器都保证具备。推荐把“Windows 10/11 x64 且已安装 WebView2 Runtime”写入交付环境，并在启动失败时给出明确提示。把 Fixed Version WebView2 一并塞进单 EXE 会额外增加 250 MB 以上并显著拖慢单文件解压启动，不适合本课程项目。

## 2. 需求基线与范围

### 2.1 必须完成的需求

1. 生成或导入一条仅含 `A/T/C/G` 的参考序列，长度 2,000-5,000 bp。
2. 生成或导入 50 条 Reads，每条 50-150 bp。
3. 生成数据时，每条 Read 独立随机包含 0-3 个替换型错配，四类错配数量随随机种子变化，Reads 的真实来源位置覆盖参考序列的不同区段。
4. 用户必须可以通过原生文件夹选择对话框指定生成数据的保存目录；取消选择不能报错或写入未知目录。
5. 待匹配数据必须支持两种导入：选择完整数据集文件夹自动识别其中的参考序列/Reads/可选真值文件，或分别选择参考序列文件和 Reads 文件。
6. 参考序列以连续字符数组形式保存在内存中，建议使用 `bytearray`，做到一个碱基一个字节且可直接按下标访问。
7. 手写拉链法哈希表，不能用 Python `dict`/`defaultdict` 充当核心 K-mer 索引。
8. 每个 K-mer 对应一个手写位置单链表，记录它在参考序列中的所有 0-based 起始下标。
9. 使用步长为 1 的滑动窗口建立 K-mer 索引，窗口总数必须等于 `N-K+1`。
10. 每条 Read 取前 K 个字符作为精确种子，查找候选位置，再逐字符计算 Hamming distance；错配数超过阈值记录剪枝，并最多继续收集到 3 个错配用于失败 Read 可视化。
11. 可视化必须展示参考坐标、成功命中区间、Read 对齐、错配字符、候选数、比较字符数和剪枝次数。
12. 软件离线可用，生成、检索和结果数据均只在本机处理。
13. 最终产物为一个可双击运行、无控制台窗口的 `.exe`；根目录现有 `icon.ico` 只引用，不移动、不重绘、不修改。

### 2.2 明确不做的内容

本项目实现的是课程规定的简化容错检索器，不应包装成完整 NGS Mapper。以下内容不在本次范围：

- 插入和删除（Hamming distance 只处理等长字符串的替换错配）。
- 反向互补链比对。
- SAM/BAM 输出、比对质量 MAPQ、成对 Reads、真实测序平台质量模型。
- 亿级 Reads 和完整人类基因组规模。
- 云端服务、账号系统、数据库和多人协作。

答辩时应主动说明这些边界。数据规模虽是教学规模，但数据结构、候选缩减、剪枝统计和复杂度分析是完整的。

### 2.3 验收口径

| 类别 | 验收条件 |
|---|---|
| 数据生成 | 输出 1 条 2,000-5,000 bp 参考序列和恰好 50 条 50-150 bp Reads；字符集合法 |
| 数据落盘 | 所有文件位于用户选择目录下新建的数据集子目录，不覆盖旧数据 |
| 输入方式 | 既可选择完整数据集文件夹自动解析，也可分别选择 FASTA 与 FASTQ |
| 文件夹解析 | 规范文件夹可直接加载；缺失或存在多个同类型候选时明确报错，不静默猜测 |
| 可复现 | 每次生成记录随机种子；用同一参数和种子可逐字节复现 |
| 位置分散 | 50 个来源起点采用分层抽样，至少覆盖参考序列全部 10 个十分位区段 |
| 错配正确 | 真值文件记录的错配数、位置和碱基变化与 FASTQ 中序列一致 |
| 索引正确 | 索引节点数为 `N-K+1`，重复 K-mer 的所有位置均可遍历得到 |
| 检索正确 | 阈值 3 时，生成数据的每条 Read 均能在真实来源位置命中 |
| 剪枝正确 | 一旦累计错配数大于阈值记录剪枝；为保留 0-3 错配失败 Read 的近似轨迹，最多比较到 3 个错配后停止 |
| 交互正确 | 工作台、索引状态、Reads 列表、报告四个页面全部来自真实结果，不残留原型假数据 |
| 离线正确 | 断网后图标、页面、生成、索引、检索、报告均可用 |
| 交付正确 | 在未安装 Python 的干净 Windows 测试机上，仅双击 EXE 即可运行 |

## 3. 最适合答辩的演示流程

界面第一目标不是功能数量，而是让老师在 3-5 分钟内看清“数据从哪里来、索引如何构建、候选如何剪枝、错配如何被允许”。推荐主流程如下：

1. 启动程序，工作台显示“尚未加载数据”，而不是原型中的固定 3,200 bp 和 47 条命中。
2. 点击“生成演示数据”，选择保存文件夹；界面展示本次随机种子、参考长度、Reads 数量和实际文件路径。
3. 生成完成后自动加载数据并自动建立 K-mer 索引；切到“索引状态”，展示窗口数、桶数、已用桶、碰撞数、最长桶链和位置节点数。
4. 以 `K=6`、最大错配 `2` 运行检索；主沙盘在参考序列不同区域显示命中带，不能只显示连续的 400-470 区间。
5. 点击一条含 2 个错配的 Read，右侧检查器逐位对齐参考序列和 Read，错配碱基着色，并展示“种子 -> 哈希桶 -> 候选位置 -> Hamming 比较 -> 接受”的路径。
6. 点击一条含 3 个错配的 Read，展示超过阈值后的提前剪枝以及未命中状态；结果保留 `nearest_hit` 供失败 Read 对齐展示。
7. 把阈值调到 4 再运行，展示原先被拒绝的 Read 变为命中，命中数单调增加。
8. 在 Reads 页面筛选、排序并查看全部 50 条结果，最后进入报告页导出 JSON/CSV 或调用系统打印为 PDF。

建议增加一个“答辩演示种子”输入框：默认可随机生成，也允许输入固定整数复现实验。随机不等于不可复现；把种子保存在真值文件和报告中，才能避免答辩时遇到不理想的随机样本。

## 4. 数据输入、生成与记录规范

### 4.1 为什么选择 FASTA + FASTQ + JSON

| 文件 | 用途 | 原因 |
|---|---|---|
| `reference.fasta` | 参考序列 | FASTA 是参考核酸序列最通用、最易手写解析的文本格式 |
| `reads.fastq` | 50 条 Reads | FASTQ 同时记录序列和逐碱基质量字符，符合 Reads 的常见交换方式 |
| `ground_truth.json` | 生成参数与真实来源 | FASTA/FASTQ 不适合保存“该 Read 从哪里截取、改了哪些位置”等教学真值 |

不建议把所有内容塞进自定义 TXT，也不建议把真值写入算法会读取的 FASTQ 标题。检索器只读取参考序列和 Read 序列；`ground_truth.json` 仅用于自动测试、演示标记和结果校验，不能帮助算法“作弊”定位。

### 4.2 数据输入的三条路径

界面需要区分“生成数据的保存地址”和“待匹配数据的输入来源”。二者都是文件夹，但语义不同，不能共用一个含糊的路径字段。

#### 路径 A：指定地址并随机生成

1. 用户点击“选择保存位置”，Python 通过原生文件夹对话框返回一个输出父目录。
2. 前端只读显示该地址，不允许把普通文本框中的任意字符串直接当作已验证路径。
3. 用户点击“生成并加载”，程序在父目录下新建唯一数据集子文件夹，写入 `reference.fasta`、`reads.fastq` 和 `ground_truth.json`。
4. 写出和自检成功后，程序立即把新数据集子文件夹交给同一套“文件夹自动解析”流程加载；用户不需要再分别选择两个文件。
5. 前端同时显示“保存父目录”和“本次数据集目录”，避免用户误以为文件直接写在父目录根部。

#### 路径 B：选择完整数据集文件夹自动解析

用户点击“选择数据集文件夹”，调用 `choose_dataset_directory()`；选择后由 `start_load_dataset_folder(directory)` 后台解析。自动发现规则必须确定且可测试：

1. 只检查用户所选目录的当前层，不递归搜索任意子目录。随机生成的数据集子文件夹本身就是可直接选择的完整目录。
2. 优先识别规范文件名 `reference.fasta`、`reads.fastq` 和可选的 `ground_truth.json`，扩展名匹配不区分大小写。
3. 如果规范文件名不存在，则在当前层分别查找 `.fa/.fasta` 和 `.fq/.fastq`。
4. 每一类恰好一个候选时可以自动采用；缺少参考序列或 Reads 时返回缺失错误。
5. 同一类有多个候选时返回歧义错误，并列出候选文件，要求用户改用“分别选择”；程序不能按文件名排序后擅自选择第一个。
6. `ground_truth.json` 是可选文件。存在时校验 schema、随机参数和 FASTA/FASTQ 校验值；不存在时仍可正常检索，只是不显示“真实来源是否找回”等生成数据专属指标。
7. 如果用户误选了只包含多个 `dna_demo_*` 子文件夹的父目录，界面提示“请选择其中一个具体数据集文件夹”，不递归猜测最新目录。

#### 路径 C：分别选择参考序列和 Reads

1. 用户分别点击“选择参考序列”和“选择 Reads”，对应原生文件对话框仅显示 `.fa/.fasta` 或 `.fq/.fastq`。
2. 两个文件可以来自不同目录；二者都存在后，“加载数据”按钮才可用。
3. 用户取消其中一次选择时，不清空另一个已经确认的字段，也不启动半完整加载。
4. 可提供折叠的“选择真值文件（可选）”，但普通外部数据不需要 `ground_truth.json`。
5. 点击加载后调用 `start_load_dataset_files({reference_path, reads_path, truth_path?})`，使用与文件夹模式完全相同的 FASTA/FASTQ 解析和业务校验。

三条路径最终都归一化为同一对象，后续服务不得根据 UI 分支重复实现索引逻辑：

```text
DatasetDescriptor
├── source_mode: generated | folder | separate_files
├── dataset_directory: Path | None
├── reference_path: Path
├── reads_path: Path
└── truth_path: Path | None
```

`DatasetService` 只接收已发现并校验路径的 `DatasetDescriptor`。加载新数据集成功后，必须清空旧索引和旧运行结果；加载失败时保留原有可用数据集，避免一次错误输入把当前展示状态破坏。

### 4.3 推荐的数据集目录

用户选择 `D:\课程设计数据` 后，程序应新建唯一子目录，而不是直接把多个文件散落到用户目录：

```text
D:\课程设计数据\
└── dna_demo_20260827_143015_seed_104729\
    ├── reference.fasta
    ├── reads.fastq
    └── ground_truth.json
```

若目录同名则追加 `_01`、`_02`，绝不静默覆盖。文件先写入同目录临时文件，全部校验成功后使用 `os.replace` 原子替换为正式文件；任一步失败时清理本次临时文件并向界面返回中文错误。

### 4.4 FASTA 规则

推荐输出：

```fasta
>chr-demo-01 synthetic_reference length=3200 seed=104729
ATCG...
```

实现约束：

- 第一行以 `>` 开头，第一个空白前是唯一序列 ID。
- 序列仅含大写 `A/T/C/G`，每行建议 80 个字符，文件末尾保留换行。
- 解析时忽略空行并拼接所有序列行；本项目只接受一条参考序列，多条时给出明确错误。
- 输入时接受 `.fa` 和 `.fasta`，校验字符集和 2,000-5,000 长度。

### 4.5 FASTQ 规则

每条记录固定四行，序列不换行：

```fastq
@READ-001
ATCG...
+
IIII...
```

实现约束：

- 第 1 行以 `@` 开头，第 3 行以 `+` 开头。
- 第 2 行为大写 `A/T/C/G`，长度 50-150。
- 第 4 行质量字符串必须与序列等长。
- 生成数据统一写 Phred+33 的 `I`，即合成质量占位值 Q40；当前 Hamming 检索不读取质量分数，报告中必须说明这一点。
- 输入时接受 `.fq` 和 `.fastq`，必须恰好解析出 50 条才能进入课程演示模式。

### 4.6 `ground_truth.json` 建议结构

坐标约定必须同时写入文件：算法内部使用 0-based、半开区间；界面显示使用 1-based、闭区间。不要在代码中混用。

```json
{
  "schema_version": 1,
  "dataset_id": "dna_demo_20260827_143015_seed_104729",
  "created_at": "2026-08-27T14:30:15+08:00",
  "random_seed": 104729,
  "coordinate_system": {
    "internal": "0-based half-open",
    "display": "1-based inclusive"
  },
  "generator": {
    "reference_length": 3200,
    "read_count": 50,
    "read_length_range": [50, 150],
    "mismatch_range": [0, 3],
    "protected_prefix_length": 12
  },
  "reads": [
    {
      "id": "READ-001",
      "length": 87,
      "reference_start_0": 412,
      "reference_start_1": 413,
      "mismatch_offsets_0": [21, 63],
      "mutations": [
        {"read_offset_0": 21, "reference_position_0": 433, "from": "A", "to": "G"},
        {"read_offset_0": 63, "reference_position_0": 475, "from": "T", "to": "C"}
      ]
    }
  ]
}
```

正式文件还应写入三个输出文件的 SHA-256 校验值。校验值可以放在 JSON 的 `checksums` 字段中，但计算 `ground_truth.json` 自身哈希会形成循环，因此只校验 FASTA 和 FASTQ，或额外生成 `checksums.sha256`。本项目采用前一种即可。

### 4.7 随机生成算法

推荐实现步骤：

1. 若用户未输入种子，用 `secrets.randbits(32)` 产生种子，再创建独立的 `random.Random(seed)`；禁止直接依赖全局随机状态。
2. 参考长度 `N = rng.randint(2000, 5000)`，逐位从 `ATCG` 均匀采样。
3. 为 50 条 Read 预先随机生成 50-150 的长度。
4. 将参考序列可用起点区间分成 50 个等比例层，每层随机选择一个来源起点，然后打乱 Read ID 与起点的对应关系。这样仍然随机，但不会把所有命中挤在同一区段。
5. 为了让 0、1、2、3、4 类别在答辩中一定出现，展示预设采用 `[0,1,2,3,4] * 10` 后随机打乱，即每类恰好 10 条。若以后增加“完全随机”模式，再使用 `randint(0,4)`。
6. 从参考序列截取原始 Read。错配位置只从 `[12, read_length)` 中无放回抽样，保护最大可选 K 值 12 的种子前缀。
7. 同一 Read 有多个错配时尽量保证错配位置间隔至少 5 bp，以便对齐图清晰；如果重试达到上限，再退化为普通无放回抽样。
8. 每个错配碱基从原碱基之外的三个碱基中随机选择，保证“修改”后一定不同。
9. 写出 FASTA、FASTQ 和真值 JSON，再用项目自己的解析器重新读回并做完整校验。
10. 用项目自己的检索引擎以阈值 3 做自检，确认 50 条 Read 的真实来源位置全部出现在命中集合中，成功后才向前端返回完成状态。

这里保护前 12 bp 是必须的工程修正。课程指定只用 Read 前 K 个字符作精确种子；若错配落入种子，真实位置不会进入候选链表，即使整条 Read 的总错配数小于阈值也会假阴性。保护最大 K 值后，用户在 K=4-12 内切换都能稳定演示。导入外部 FASTQ 时无法保证这一点，界面应把“前 K 位需精确一致”作为算法限制显示在运行元数据中。

### 4.8 多命中与真值的处理

随机 DNA 仍可能出现重复种子或偶然相似区域，因此一条 Read 可能有多个合法命中。后端必须返回 `hits` 数组，不能只保留第一个位置。界面可默认选中 Hamming distance 最小、位置最靠前的 `best_hit`，同时显示“共 N 个命中”。对于生成数据，还要标记真实来源是否包含在 `hits` 中，但真值不能参与候选搜索。

## 5. 核心数据结构与算法设计

### 5.1 连续参考序列

使用 `bytearray` 存储大写 ASCII 碱基。它满足连续、逐字节、按下标访问的要求，也比 Python Unicode 字符串更容易向老师解释“连续字符数组”。FASTA 解析器完成校验后再构造 `bytearray`。

### 5.2 手写拉链法哈希表

建议实现两类链表节点：

```text
buckets: list[KmerEntry | None]

KmerEntry
├── key: bytes[K]
├── positions_head: PositionNode | None
├── positions_tail: PositionNode | None
└── next: KmerEntry | None       # 同一个桶中的冲突链

PositionNode
├── position: int
└── next: PositionNode | None    # 同一个 K-mer 的位置链
```

`list` 只用于固定桶数组，允许使用；禁止用 `dict`、`set` 或 `defaultdict` 代替上述结构。返回给前端的普通 JSON 字典不属于核心索引，可以使用 Python `dict` 做序列化。

哈希函数不要调用 Python 内置 `hash()`，因为其字符串哈希带进程随机化，不便复现实验。可以手写 2-bit 编码：`A=0, C=1, G=2, T=3`，循环执行 `code = code * 4 + base_code`，最后 `code % bucket_count`。所有 K-mer 长度固定，因此前导 A 不会造成键歧义；模运算造成的冲突由桶链表处理。

桶数不宜机械设成窗口数的两倍。K=6 时完整 key 空间只有 `4^6=4096`，桶太多会让 2-bit 编码几乎不发生不同 key 的桶冲突，拉链法在答辩中反而难以观察。教学数据建议采用 `next_prime(max(53, min(window_count // 2, (4 ** K) // 2)))`，使典型装载因子约为 1-2，查找链仍很短且能稳定展示冲突。质数判断逻辑应手写；参考长度已知，因此无需动态扩容。索引页需要展示：

- `bucket_count`
- `used_bucket_count`
- `unique_kmer_count`
- `position_node_count`，必须等于 `N-K+1`
- `collision_count`
- `max_bucket_chain_length`
- `load_factor`
- `build_elapsed_ms`

### 5.3 索引构建

对 `i = 0 ... N-K`：

1. 取 `reference[i:i+K]` 作为 K-mer。
2. 计算自定义哈希桶下标。
3. 遍历桶冲突链；若已有相同 key，就在其位置链尾部追加 `i`。
4. 若不存在，则新建 `KmerEntry` 和首个 `PositionNode`。

位置链保留 `head` 和 `tail`，可 O(1) 追加并保持坐标升序。不要采用头插法后再依赖 Python 排序，这样算法轨迹更容易解释。

### 5.4 批量容错检索

每条 Read 的检索步骤：

1. 校验 `len(read) >= K`。
2. 截取 `read[0:K]` 作为精确种子。
3. 在哈希表中找到对应 K-mer 的位置链。
4. 逐个遍历候选位置；若 `candidate + read_length > N`，记录边界拒绝。
5. 从第 0 位开始逐字符比较参考序列与 Read。
6. 每次不同就累计错配并保存 `{offset, reference_base, read_base}`。
7. 当累计错配 `> max_mismatches` 时立即退出该候选，并增加 `pruned_candidate_count`。
8. 完整比较后若错配数 `<= max_mismatches`，保存该命中区间和错配明细。

每条 Read 返回的不只是命中位置，还包括：种子、桶下标、候选总数、越界候选数、已比较字符总数、提前剪枝数、全部命中、最佳命中、被拒绝时的最佳近似位置（`nearest_hit`）和耗时。批次结果汇总 50 条 Reads、命中/未命中数、0-3 错配分布及其占总量比例、候选总数、字符比较数、剪枝率和总耗时。

时间复杂度：构建索引约为 `O((N-K+1) * K)`，K 很小且固定；检索约为 `O(R * (K + C * L))`，其中 R 是 Reads 数、C 是每条 Read 的种子候选数、L 是 Read 长度。空间复杂度约为 `O(N + (N-K+1))`。报告中还应给出直接逐位置比较的理论比较规模，说明局部索引如何降低候选数。

### 5.5 哪些结构必须手写，哪些内置能力可以使用

课程文本明确点名的核心结构是“拉链法哈希表”和“单链表位置集合”，这两部分必须自行定义节点、哈希、插入、查找、遍历和统计。建议在 `docs/architecture.md` 中保留下面的边界表，答辩时也按此说明：

| 场景 | 可以使用 | 禁止替代 |
|---|---|---|
| 固定哈希桶数组 | Python `list`，仅按下标保存桶头节点 | 用 `dict/defaultdict` 从 K-mer 直接映射位置 |
| 位置集合 | 自定义 `PositionNode` 单链表 | 用 `list/set/tuple` 直接保存某 K-mer 的全部位置 |
| 桶内冲突处理 | 自定义 `KmerEntry.next` 冲突链 | 用字典嵌套字典或第三方哈希容器 |
| 参考序列 | `bytearray` 作为连续一字节数组，并封装访问方法 | 用 DataFrame、数据库或现成生物信息学索引 |
| 哈希函数 | 自行实现 2-bit 编码和取模 | 直接调用 `hash(kmer)` |
| 结果传输 | 普通 `dict/list` 组装 JSON | 把传输用字典反过来当作搜索索引 |
| 排序/分页 | 前端数组或结果层列表 | 在核心构建过程中先收集列表再伪装成链表 |

`networkx` 与本题无关，也不能用于替代结构。`dataclass` 只是生成普通数据类样板代码，理论上可以使用；但核心链表节点建议写成带 `__slots__` 的普通类，字段和指针关系更直观，也能避免节点被随意附加属性。

### 5.6 连续字符数组的实现思路

在 `core/genome_buffer.py` 增加一个很薄的 `GenomeBuffer` 包装类，比在各处直接操作字符串更符合题意；该文件已经纳入第 7 节目录树和阶段 2 的实施范围。

```text
GenomeBuffer
├── _data: bytearray
├── length: int
├── base_at(index) -> int
├── window(start, length) -> bytes
└── validate_range(start, length)
```

构造时先创建固定长度的 `bytearray(len(sequence))`，再逐下标校验并写入 ASCII 值；不保存第二份完整字符串。对外不暴露 `_data`，索引和匹配器通过 `base_at` 或受检查的窗口访问。这样可以清楚展示：参考序列是连续存储，而哈希索引只保存 K-mer 和整数位置，不复制整条参考序列。

若老师只要求“使用连续字符数组”而未要求手写数组扩容，`bytearray` 作为底层固定数组是合理的；不要自行实现一套低价值的 Python 动态数组。真正需要手写并评分的是上层哈希和链表逻辑。

### 5.7 节点与哈希表的建议字段

核心类可以按下面的最小字段实现：

```python
class PositionNode:
    __slots__ = ("position", "next")

    def __init__(self, position):
        self.position = position
        self.next = None


class KmerEntry:
    __slots__ = ("key", "positions_head", "positions_tail", "position_count", "next")

    def __init__(self, key, position):
        node = PositionNode(position)
        self.key = key
        self.positions_head = node
        self.positions_tail = node
        self.position_count = 1
        self.next = None


class ChainedHashTable:
    __slots__ = (
        "_buckets", "bucket_count", "unique_key_count", "position_node_count",
        "used_bucket_count", "collision_count", "max_bucket_chain_length"
    )
```

这是实现草图，不要求现在逐字照抄，但字段含义应保持稳定。`KmerEntry.next` 和 `PositionNode.next` 是两种不同指针：前者解决不同 K-mer 哈希到同一桶的冲突，后者记录同一个 K-mer 的多个参考位置。界面中的结构示意也应区分这两条链。

### 5.8 手写哈希函数思路

不需要维护碱基到数字的 Python 字典，可以写一个普通分支函数：

```text
base_code('A') = 0
base_code('C') = 1
base_code('G') = 2
base_code('T') = 3
其他字符 -> 抛出非法碱基错误
```

K-mer 的哈希计算伪代码：

```text
function bucket_index(kmer):
    code = 0
    for base in kmer:
        code = code * 4 + base_code(base)
    return code mod bucket_count
```

所有键长度相同，因此 2-bit/base-4 编码在取模前是确定的。即使两个不同 K-mer 取模后得到同一桶，也只形成正常哈希冲突；查找时必须继续逐节点比较完整 key，不能仅凭桶号判断相等。

桶容量在建立索引前已知，可一次性设为 `next_prime(max(53, min(window_count // 2, (4 ** K) // 2)))`，本项目不必实现动态扩容。`next_prime` 从候选奇数开始，用试除到平方根的手写 `is_prime` 寻找下一个质数。这个容量策略是有意选择的教学型折中：典型冲突链短，但不同 key 的冲突足以在索引页观察；固定容量还让一次运行内的节点和统计可复现。

### 5.9 `insert`、`find` 与遍历伪代码

插入一个 K-mer 位置：

```text
function insert(kmer, position):
    index = bucket_index(kmer)
    current = buckets[index]

    if current is null:
        buckets[index] = new KmerEntry(kmer, position)
        used_bucket_count += 1
        unique_key_count += 1
        position_node_count += 1
        update max_bucket_chain_length
        return

    chain_length = 0
    previous = null
    while current is not null:
        chain_length += 1
        if current.key equals kmer:
            current.positions_tail.next = new PositionNode(position)
            current.positions_tail = current.positions_tail.next
            current.position_count += 1
            position_node_count += 1
            return
        previous = current
        current = current.next

    previous.next = new KmerEntry(kmer, position)
    unique_key_count += 1
    position_node_count += 1
    collision_count += 1
    update max_bucket_chain_length with chain_length + 1
```

查找 K-mer：

```text
function find_entry(kmer):
    current = buckets[bucket_index(kmer)]
    while current is not null:
        if current.key equals kmer:
            return current
        current = current.next
    return null
```

遍历位置时返回一个迭代器或逐节点回调，不要先复制成列表再交给匹配器：

```text
node = entry.positions_head
while node is not null:
    compare read at node.position
    node = node.next
```

只有在最终准备 JSON 响应时，才把命中结果转换为普通列表。

### 5.10 索引构建和检索的完整伪代码

索引构建：

```text
function build_index(reference, K):
    require 1 <= K <= reference.length
    window_count = reference.length - K + 1
    target = max(53, min(window_count // 2, (4 ** K) // 2))
    table = ChainedHashTable(next_prime(target))

    for start from 0 to window_count - 1:
        kmer = reference.window(start, K)
        table.insert(kmer, start)

    assert table.position_node_count == window_count
    return KmerIndex(reference, K, table)
```

单条 Read 检索：

```text
function search_read(index, read, threshold):
    require read.length >= index.K
    seed = read[0:index.K]
    entry = index.table.find_entry(seed)
    result = empty ReadSearchResult

    if entry is null:
        return result with reason SEED_NOT_FOUND

    candidate_node = entry.positions_head
    while candidate_node is not null:
        start = candidate_node.position
        result.candidate_count += 1

        if start + read.length > reference.length:
            result.out_of_bounds_count += 1
            candidate_node = candidate_node.next
            continue

        mismatch_count = 0
        mismatch_details = empty output list

        for offset from 0 to read.length - 1:
            result.compared_base_count += 1
            if reference[start + offset] != read[offset]:
                mismatch_count += 1
                append mismatch detail for visualization
                if mismatch_count > threshold:
                    result.pruned_candidate_count += 1
                    stop comparing this candidate

        if comparison reached the end and mismatch_count <= threshold:
            append Hit(start, mismatch_count, mismatch_details)

        candidate_node = candidate_node.next

    choose best_hit from hits without deleting other hits
    return result
```

为了精确统计剪枝，循环需要区分“首次超过阈值并记录剪枝”“超过 3 个错配后的 break”和“自然比较到末尾”。不能只在循环后检查 `mismatch_count`，否则会把已剪枝候选误计为完整比较。

### 5.11 必须持续成立的结构不变量

每次构建后执行一个只在测试/调试模式启用的 `validate()`，检查：

1. `_buckets` 长度始终等于 `bucket_count`。
2. 每个 `KmerEntry` 只出现在一个桶冲突链中，且其哈希下标等于所在桶下标。
3. 同一桶链内不能存在两个 key 相同的 `KmerEntry`；相同 key 必须共享一条位置链。
4. 空位置链不允许存在；`positions_head is None` 当且仅当 `positions_tail is None`，实际索引条目中二者都不能为 None。
5. 每条位置链的末节点必须等于 `positions_tail` 且 `tail.next is None`。
6. 每条位置链中的位置严格递增，且都在 `0 <= position <= N-K` 范围内。
7. 所有位置链节点总数等于 `position_node_count`，构建完成后还必须等于 `N-K+1`。
8. 桶链中的条目总数等于 `unique_key_count`，非空桶数等于 `used_bucket_count`。
9. `collision_count` 的定义固定为“插入新 key 时目标桶已非空的次数”，报告和测试都采用同一定义。

`validate()` 可以使用慢速完整遍历，因为只面对最多约 5,000 个窗口且不在正式检索热路径执行。不要为了验证方便另建一个字典作为第二套真索引；测试中的期望数据可以使用普通 Python 容器，生产核心不能。

### 5.12 手写结构的专项测试与答辩展示

除第 12 节的通用测试外，手写结构应有以下定向测试：

- 创建 1 个桶，插入多个不同 K-mer，强制验证冲突链插入和查找。
- 多次插入同一个 K-mer，确认只增加位置节点、不增加唯一 key 数和碰撞数。
- 构造 `AAAAAA...` 参考序列，验证一个 K-mer 对应长位置链以及首尾位置。
- 构造两个哈希值同桶但 key 不同的 K-mer，查找不存在的第三个 key，验证会遍历到链尾并返回 None。
- 验证位置采用尾插且严格递增。
- 对 `core/` 做代码审查，确保没有用 `dict/defaultdict/set` 保存 K-mer 或位置集合。
- 索引页选取一个真实冲突桶，画出“桶头 -> KmerEntry -> KmerEntry”和其中一个条目的“positions_head -> PositionNode -> ...”，作为手写结构的直接证据。

答辩展示不需要把 Python 对象地址暴露给用户。展示桶号、K-mer、冲突链长度、位置链内容和节点计数即可，重点是结构关系和操作过程可以从结果追溯。

## 6. 技术栈建议

### 6.1 生产依赖

| 技术 | 用途 | 选择理由 |
|---|---|---|
| Python 3.13.15 x64 | 算法、文件、服务和桌面入口 | 已在本项目虚拟环境完成精确版本的 WebView2 与 Bridge 冒烟 |
| pywebview 6.2.1 | 承载 `prd` HTML，并提供 JS/Python 双向桥接 | 不需要前端构建工具，也不需要对外 HTTP API |
| pythonnet 3.1.0 | pywebview 的 Windows/.NET 桥接 | 已取得 CPython 3.13 Windows wheel 并实测加载成功 |
| Windows WebView2 | 渲染现代 HTML/CSS/JS | Windows 上的现代 Chromium 渲染器 |
| PyInstaller 6.22.2 | 生成单 EXE | 可打包解释器、Python 依赖和静态资源 |

`pyproject.toml` 的直接运行依赖只需声明 pywebview；`requirements.lock` 必须把解析得到的 pythonnet 3.1.0 及其依赖一并锁定。不要手工复制 pythonnet 或 WebView2Loader DLL，也不要在完成打包验证后随意升级单个间接依赖。

### 6.2 开发依赖

- `pytest`：单元测试和集成测试。
- `pytest-cov`：覆盖率报告，核心算法建议达到 90% 以上分支覆盖率。
- `ruff`：格式、导入和静态规则检查。
- `pyinstaller`：仅放开发/构建依赖，不作为运行依赖。

不建议引入 Biopython。FASTA/FASTQ 解析范围很小，手写严格解析器既能减少打包依赖，也更符合课程对工程实现的考查。它不属于被禁止的核心哈希结构，但没有必要使用。

### 6.3 对既有 pywebview 方法论的取舍

`D:\工具开发\pywebview桌面应用与EXE打包实践.md` 与 pywebview/PyInstaller 官方行为一致，而且已经在多个本地工具中重复验证。结合 pywebview 官方最新架构文档，本项目同时修正了页面加载协议和线程边界。具体取舍如下：

| 方法论条目 | 本项目结论 |
|---|---|
| 轻量 `app.py` 入口，窗口/API/服务/资源分层 | 采用，便于 PyInstaller 稳定分析入口 |
| 绝对本地路径 + pywebview 内置 HTTP Server | 采用，保持资源路径独立于当前工作目录 |
| API 显式保存 `create_window` 返回对象 | 采用，不依赖 `webview.windows[0]` |
| `pywebviewready` + 限时主动探测 | 采用，避免源码与 EXE 注入时序差异 |
| API 快速启动后台任务并返回 | 生成、导入、建索引、检索采用统一任务控制器 |
| 单飞状态轮询，推送只作可选优化 | 采用，后台线程不直接操作 WebView，轮询是权威状态通道 |
| 错误只写控制台 | 明确禁止，无控制台 EXE 的错误必须进入界面状态 |
| `sys._MEIPASS` 与 spec `datas` 对齐 | 采用，集中放入 `paths.py`，不依赖当前工作目录 |
| 子进程 `CREATE_NO_WINDOW` | 当前运行时没有子进程，暂不增加；未来调用外部程序时必须采用 |
| 单实例 | 采用，避免重复启动同时写同一数据集或覆盖状态 |
| 打包后真实流程冒烟 | 采用，不能以源码运行成功替代 EXE 验收 |

本项目数据只有 2,000-5,000 bp 和 50 条 Reads，任务通常很快；后台任务架构的目的不是伪造进度，而是统一按钮反馈、异常处理和打包后桥接行为。`elapsed_ms` 仍只统计真实算法段，排队、前端动画和状态轮询时间不得计入算法性能。

### 6.4 桌面启动和资源加载

生产启动入口采用明确的窗口绑定和 pywebview 内置 HTTP Server：

```python
api = DesktopApi(task_controller)
html_path = resource_path("resources", "web", "index.html")
window = webview.create_window(
    title="NEXUS DNA Retrieval Engine",
    url=str(html_path.resolve()),
    js_api=api,
    width=1440,
    height=900,
    min_size=(1180, 720),
    resizable=True,
    text_select=False,
)
api._bind_window(window)
webview.start(gui="edgechromium", debug=False, http_server=True)
```

关键规则：

1. 根目录 `app.py` 只导入并调用 `desktop.main.main()`，不放业务逻辑。
2. `webview.start()` 必须在主线程进入 GUI 事件循环。
3. `DesktopApi` 只使用私有 `_bind_window()` 保存的明确窗口做文件对话框，不读取 `webview.windows[0]`；私有命名同时阻止 pywebview 把原生窗口递归暴露给 JS API。
4. 使用绝对本地路径交给 pywebview，并启用内置 HTTP Server；不要通过 `Path.as_uri()` 强制进入 `file://`，官方文档指出该协议在 JS Bridge 和资源处理上存在限制。
5. 内置 Server 只监听本机随机端口，不需要外网访问或额外 Flask 服务；页面卸载时由 pywebview 负责生命周期清理。
6. `paths.py` 同时处理源码目录与 `sys._MEIPASS`，所有资源路径都从该模块取得，禁止使用 `Path.cwd()` 推断资源位置。
7. 开发模式可以打开 DevTools；发布构建固定 `debug=False`。

### 6.5 前端等待 Bridge 的正确方式

JavaScript 使用官方事件和可取消、带退避的短探测，并由单一轮询调度器负责后续状态同步：

```javascript
async function connectDesktopBridge() {
  const api = window.pywebview?.api;
  if (!api || typeof api.get_state !== "function") return false;
  renderAppState(await api.get_state());
  return true;
}

window.addEventListener("pywebviewready", connectDesktopBridge);

function probeBridge(delay) {
  window.setTimeout(async function () {
    if (await connectDesktopBridge()) return;
    probeBridge(Math.min(3000, delay * 2));
  }, delay);
}
probeBridge(100);
```

所有调用经过统一入口，每次读取当前 API，不缓存第一次可能被拒绝的 Promise：

```javascript
async function callDesktopApi(method, ...args) {
  const api = window.pywebview?.api;
  if (!api || typeof api[method] !== "function") {
    throw new Error("本地服务尚未就绪，请稍后重试");
  }
  return api[method](...args);
}
```

按钮点击后应先更新可见状态并禁用重复操作，再 `await` API。关键按钮继续使用 `addEventListener`，但自动化测试必须证明监听器已安装；不要定义 `value`、`status`、`name` 等过于泛化的全局函数，也不要让元素 `id/name` 与函数名冲突。

### 6.6 后台任务、状态同步和错误可见性

`DesktopApi` 不直接执行生成或检索，只做参数转换、调用任务控制器和返回响应。`TaskController` 同一时间只运行一个任务，并用 `threading.Lock` 保护如下状态：

```text
task_id、task_type、status、phase、message
progress、total、completed、error、result_version
```

推荐状态为 `idle -> validating -> running -> completed/error/cancelled`。`start_generate_dataset`、`start_load_dataset_folder`、`start_load_dataset_files`、`start_search` 必须快速返回 `{ok: true, task_id: ...}`；索引随数据加载或检索参数变化自动构建，不再公开手动重建 API。后台 worker 调用既有服务，并把最终大结果留在 Python 侧，由 `get_last_result()` 或 `get_read_detail()` 按需读取。

后台任务只更新线程安全状态；前端通过单飞 `get_state()` 轮询读取，运行期间 250 ms、空闲时 1 s、页面隐藏时 3 s，并对超时/异常采用退避。不要从 worker 线程直接调用 `evaluate_js`，避免跨线程操作 WebView2。若未来启用事件推送，它只能作为可选即时优化，失败不能影响任务。

所有 Bridge 响应统一使用 `{ "ok": true, "data": ... }` 或 `{ "ok": false, "error": { "code": ..., "message": ... } }`。Python 日志保留堆栈用于排障，界面显示经过整理的中文错误、失败阶段和恢复动作。生成/运行按钮至少要可见显示“正在校验、正在生成/构建/检索、已完成、已取消、失败”。

pywebview 暴露方法本身会在独立线程执行且不保证线程安全，因此 API 状态、当前数据集、当前索引和最近结果都不能无锁并发修改。窗口关闭时设置取消标志；worker 应在阶段边界检查取消。后台线程结束后的事件推送若因窗口销毁失败，应安全忽略。

文件/文件夹选择仍由 Python 调用绑定窗口的 `create_file_dialog(FileDialog.FOLDER/OPEN/SAVE)`。对话框调用是短交互，不放进后台 worker。后端返回的数据使用 `textContent` 和 DOM API 渲染；原型中 `innerHTML` 只保留固定模板，文件名、路径、Read ID 等外部值不得直接拼入 HTML。

### 6.7 前端离线化

当前四个 HTML 页面都从 `https://unpkg.com/lucide@0.468.0/...` 加载图标。单 EXE 和“仅本地”要求下，这会导致断网后图标丢失。实现时应把固定版本 `lucide.min.js` 和其许可证放到包内 `resources/web/vendor/`，HTML 改为相对路径；生产页面不得发出任何网络请求。

`prd/` 继续保留为设计原型和对照，不直接在其中堆积后端逻辑。把生产副本放到 Python 包的 `resources/web/` 下，再逐步移除：

- 固定参考序列和固定 Reads。
- 固定 47/50 命中、18.4 ms 等指标。
- 所有“将在 pywebview 版本中接入”的 toast 占位行为。
- Reads 页 `buildReadData()` 的虚构数据。

## 7. 建议项目文件架构

```text
DNA_retrieval_engine/
├── icon.ico                         # 已有文件，只引用，不修改
├── app.py                           # 稳定轻入口，供源码运行和 PyInstaller 分析
├── README.md
├── pyproject.toml                   # 项目信息、运行依赖、开发工具配置
├── requirements.lock               # 最终构建环境的精确锁定版本
├── .gitignore
├── 课程设计.txt
├── 技术路径调研.md
├── prd/                             # 现有原型，保留为设计基线
├── docs/
│   ├── architecture.md              # 数据流、数据结构、复杂度说明
│   ├── data-format.md               # FASTA/FASTQ/ground_truth 约定
│   ├── demo-script.md               # 3-5 分钟答辩演示脚本
│   └── release-checklist.md          # 干净机器交付检查表
├── src/
│   └── dna_retrieval_engine/
│       ├── __init__.py
│       ├── __main__.py              # 转调 desktop.main，支持 `python -m`
│       ├── config.py                # K 范围、阈值、数据规模等常量
│       ├── models.py                # dataclass：Read、Hit、RunResult 等
│       ├── paths.py                 # 源码/sys._MEIPASS 资源路径，禁止依赖 cwd
│       ├── desktop/
│       │   ├── __init__.py
│       │   ├── main.py              # 创建窗口、绑定 API、主线程启动 WebView2
│       │   ├── api.py               # DesktopApi：参数转换、对话框、任务调度
│       │   ├── task_controller.py   # 单任务、锁、状态、推送和取消
│       │   └── single_instance.py   # Windows 命名互斥量和重复启动提示
│       ├── core/
│       │   ├── __init__.py
│       │   ├── genome_buffer.py      # 连续 bytearray 参考序列及边界访问
│       │   ├── linked_list.py       # PositionNode 及遍历逻辑
│       │   ├── hash_table.py        # KmerEntry、手写哈希表、统计信息
│       │   ├── kmer_index.py        # 滑动窗口构建/重建索引
│       │   └── matcher.py           # Hamming 比对、剪枝、批量检索
│       ├── services/
│       │   ├── __init__.py
│       │   ├── dataset_generator.py # 随机参考、分散 Reads、错配和真值
│       │   ├── dataset_service.py   # 生成/导入/校验/当前数据集状态
│       │   ├── search_service.py    # 串联索引、检索、结果缓存和锁
│       │   └── report_service.py    # JSON/CSV/可打印报告导出
│       ├── storage/
│       │   ├── __init__.py
│       │   ├── dataset_discovery.py # 文件夹非递归发现、歧义处理和 Descriptor
│       │   ├── fasta.py             # 严格 FASTA 读写
│       │   ├── fastq.py             # 严格 FASTQ 读写
│       │   ├── truth.py             # ground_truth JSON 读写
│       │   └── result_export.py     # 结果 JSON/CSV 写出
│       └── resources/
│           └── web/                 # 从 prd 迁移的可信、离线生产前端
│               ├── index.html
│               ├── app.js
│               ├── bridge-client.js # ready 探测、统一调用、推送和轮询
│               ├── state-store.js   # 四页面共享状态适配
│               ├── styles.css
│               ├── pages.js
│               ├── pages.css
│               ├── pages/
│               │   ├── index-state.html
│               │   ├── reads.html
│               │   └── report.html
│               └── vendor/
│                   ├── lucide.min.js
│                   └── LUCIDE-LICENSE.txt
├── tests/
│   ├── unit/
│   │   ├── test_dataset_discovery.py
│   │   ├── test_genome_buffer.py
│   │   ├── test_linked_list.py
│   │   ├── test_hash_table.py
│   │   ├── test_kmer_index.py
│   │   ├── test_matcher.py
│   │   ├── test_fasta.py
│   │   ├── test_fastq.py
│   │   └── test_dataset_generator.py
│   ├── integration/
│   │   ├── test_generate_search_flow.py
│   │   ├── test_input_modes_flow.py
│   │   ├── test_bridge_contract.py
│   │   ├── test_task_state_flow.py
│   │   └── test_resource_contract.py
│   └── fixtures/                    # 小型、可审查的固定测试数据
├── packaging/
│   └── DNA_Retrieval_Engine.spec    # 固定 datas、icon、hidden imports、excludes
└── scripts/
    ├── bootstrap.ps1                # 创建 3.13.15 venv 并安装锁定依赖
    ├── check.ps1                    # ruff + pytest
    └── build.ps1                    # onedir 冒烟后构建 onefile
```

当前 `.gitignore` 忽略全部 `*.spec`。正式实施时需要增加 `!packaging/DNA_Retrieval_Engine.spec`，让经过验证的打包配置进入版本控制，而不是每次临时生成。

## 8. Python/JavaScript 接口建议

`desktop/api.py` 只做边界工作，算法不直接写在 `DesktopApi` 中。API 先通过私有 `_bind_window(window)` 保存明确窗口，所有服务属性也以下划线开头，再公开以下方法：

| JS API | 调用的服务 | 返回内容 |
|---|---|---|
| `get_state()` | TaskController/Dataset/SearchService | 桥接就绪、当前任务、数据集、索引和最近运行摘要 |
| `choose_output_directory()` | pywebview Window | 用户选择的绝对目录或取消状态 |
| `choose_dataset_directory()` | pywebview Window | 待匹配数据集文件夹或取消状态 |
| `choose_reference_file()` | pywebview Window | `.fa/.fasta` 路径 |
| `choose_reads_file()` | pywebview Window | `.fq/.fastq` 路径 |
| `choose_truth_file()` | pywebview Window | 可选 `ground_truth.json` 路径 |
| `start_generate_dataset(options)` | TaskController -> DatasetGenerator | 立即返回任务 ID，后台生成/校验/加载 |
| `start_load_dataset_folder(directory)` | TaskController -> DatasetDiscovery/Service | 自动发现当前层文件并加载 |
| `start_load_dataset_files(paths)` | TaskController -> DatasetService | 加载分别选择的参考、Reads 和可选真值 |
| `start_search(options)` | TaskController -> SearchService | 立即返回任务 ID，后台完成批量检索 |
| `cancel_task(task_id)` | TaskController | 请求在下一个安全阶段取消当前任务 |
| `get_last_result()` | SearchService | 最近完成批次的摘要和 50 条结构化结果 |
| `get_read_detail(read_id)` | SearchService | 该 Read 的全部候选/命中/错配轨迹 |
| `start_export_results(options)` | TaskController -> ReportService | 立即返回任务 ID，后台导出并在状态中给出路径 |

`start_*` 只返回接受/拒绝和任务 ID；`get_state()` 返回轻量任务状态；任务完成后，`get_last_result()` 建议返回以下检索结构：

```json
{
  "run_id": "run-20260827-143102-001",
  "parameters": {"k": 6, "max_mismatches": 2, "early_prune": true},
  "summary": {
    "read_count": 50,
    "matched_read_count": 30,
    "candidate_count": 97,
    "pruned_candidate_count": 67,
    "compared_base_count": 4210,
    "elapsed_ms": 2.731
  },
  "reads": [
    {
      "id": "READ-001",
      "seed": "ATCGTA",
      "bucket_index": 842,
      "candidate_count": 3,
      "hits": [
        {
          "start_0": 412,
          "start_1": 413,
          "end_1": 499,
          "hamming_distance": 2,
          "mismatches": []
        }
      ]
    }
  ]
}
```

不要让四个页面各自维护一份业务状态。`bridge-client.js` 负责 Bridge 就绪、统一 API 调用、单飞轮询和生命周期清理；`state-store.js` 只把 Python 状态映射到页面，`ui-state.js` 负责 capability 权限矩阵。`sessionStorage` 只保存当前筛选、选中 Read、缩放窗口等非关键视图状态；数据集、索引、任务和运行结果以 Python 服务内状态为唯一事实来源，每个页面加载后调用 `get_state()` 恢复。

## 9. 原型四个页面的接入方式

### 9.1 运行工作台 `resources/web/index.html` / `resources/web/app.js`

- 在“输入数据”增加“生成数据”和“导入数据”分段控件；导入区再提供“数据集文件夹”和“分别选择文件”两种模式。
- 生成区提供保存文件夹、随机种子（可选）、只读范围说明和“生成并加载”按钮。
- 生成区的文件夹按钮调用 `choose_output_directory()`；成功生成后直接走文件夹自动解析并加载。
- 导入文件夹模式调用 `choose_dataset_directory()` 和 `start_load_dataset_folder()`，展示识别到的参考、Reads、可选真值及警告。
- 分别选择模式提供参考序列与 Reads 两个独立文件按钮，两者齐备后调用 `start_load_dataset_files()`；不要做可手输路径的普通文本框。
- “生成并加载”调用 `start_generate_dataset`，“运行批量检索”调用 `start_search`；两者立即进入可见任务状态，完成后调用 `get_last_result` 重画 overview、序列窗口、Read 行、候选表和 Inspector。
- 将当前仅有 6 条的 `reads` 常量彻底删除，所有计数由后端汇总值驱动。
- overview 以全部参考长度为坐标，散布显示 50 条结果；局部序列仍按窗口渲染，避免一次创建 5,000 个以上复杂 DOM 节点。

### 9.2 索引状态 `resources/web/pages/index-state.html`

- 索引页为只读状态视图；索引由数据加载和检索流程自动构建，避免用户手动重复重建。
- 固定的 3,195 窗口、1,962 桶等改为真实统计。
- 桶分布表展示真实最长的前 N 个桶，但核心后端仍通过手写链表遍历得出，不使用字典聚合索引。
- 增加完整性检查：位置节点数是否等于 `N-K+1`。

### 9.3 Reads 列表 `resources/web/pages/reads.html` / `resources/web/pages.js`

- 删除 `buildReadData()`，改为真实 50 条结果。
- 保留搜索、排序、分页；增加“全部、命中、未命中、0-3 错配”筛选。
- 点击一行回到工作台并定位对应 Read。
- 表格序列长度是 50-150，不能继续把表头写死为 30 bp；长序列应省略显示并在详情中完整展开。

### 9.4 运行报告 `resources/web/pages/report.html`

- 元数据写入数据集 ID、随机种子、生成/导入文件、K、阈值、参考长度和坐标规则。
- 统计 0-3 错配分布及其占 50 条 Reads 的比例、匹配率、候选缩减、剪枝率和真实耗时。
- MVP 可靠导出格式为 `run_report.json` 和 `reads_results.csv`。
- “打印/PDF”可调用 `window.print()` 交给 Windows 打印对话框选择“Microsoft Print to PDF”；不要在未验证的情况下宣称已直接生成中文 PDF。

## 10. 分阶段实施路径：每一步做什么、怎么做、在哪做

### 阶段 0：固化工程基线

涉及文件：`app.py`、`pyproject.toml`、`requirements.lock`、`.gitignore`、`scripts/bootstrap.ps1`。

- 创建干净 Python 3.13.15 x64 虚拟环境，锁定 pywebview 6.2.1、pythonnet 3.1.0 和 PyInstaller 6.22.2。
- 配置 pytest、coverage 和 ruff。
- 允许提交 `packaging/*.spec`。
- 创建只转调 `dna_retrieval_engine.desktop.main.main()` 的根入口 `app.py`。
- 验收：`python app.py` 和 `python -m dna_retrieval_engine` 都能启动同一个最小窗口。

### 阶段 1：定义模型和文件格式

涉及文件：`models.py`、`storage/dataset_discovery.py`、`storage/fasta.py`、`storage/fastq.py`、`storage/truth.py`、`docs/data-format.md`。

- 先定义 `ReadRecord`、`Mismatch`、`Hit`、`ReadSearchResult`、`BatchResult` 等 dataclass。
- 实现严格 FASTA/FASTQ 读写和统一中文校验错误。
- 实现文件夹当前层发现、规范文件名优先、缺失/多候选歧义错误，并归一化为 `DatasetDescriptor`。
- 明确 0-based 内部坐标和 1-based 展示坐标。
- 验收：写出后可以读回且数据一致，坏文件均有可理解错误。

### 阶段 2：实现手写数据结构

涉及文件：`core/genome_buffer.py`、`core/linked_list.py`、`core/hash_table.py`、`tests/unit/test_genome_buffer.py`、`tests/unit/test_linked_list.py`、`tests/unit/test_hash_table.py`。

- 先写连续字符数组包装，再写节点和遍历，最后写哈希函数、插入、查找和统计。
- 用很小桶数在测试中强制制造冲突，验证桶链和位置链是两层不同链表。
- 验收：核心索引实现中不存在 `dict`/`set` 作为存储结构；重复 K-mer 全部位置可按升序取得。

### 阶段 3：建立索引与检索算法

涉及文件：`core/kmer_index.py`、`core/matcher.py`、对应单元测试。

- 实现滑动窗口、索引重建、Hamming 比较和超过阈值剪枝。
- 返回可视化需要的轨迹统计，不在前端推算算法数据。
- 验收：覆盖首尾边界、重复 K-mer、无候选、多个候选、0/阈值/阈值+1 错配、种子错配限制。

### 阶段 4：实现展示型随机数据生成

涉及文件：`services/dataset_generator.py`、`storage/*`、`tests/unit/test_dataset_generator.py`。

- 按第 4.7 节实现分层位置、均衡错配类别、保护前 12 bp、随机种子和真值。
- 保存到用户目录的新子目录，写后通过同一个 `dataset_discovery` 流程读回并用检索器自检，随后自动成为当前数据集。
- 验收：批量运行至少 100 个种子，所有范围和真值校验都通过，阈值 3 总能找回 50 个真实来源。

### 阶段 5：服务层、任务状态和 pywebview Bridge

涉及文件：`services/dataset_service.py`、`services/search_service.py`、`services/report_service.py`、`desktop/api.py`、`desktop/task_controller.py`、`desktop/main.py`、`desktop/single_instance.py`、`paths.py`。

- 服务层持有当前数据集、当前索引和最近结果。
- `DesktopApi` 只做输入验证、对话框、任务调度和 JSON 响应封装，并显式绑定 `create_window` 返回对象。
- `TaskController` 用锁维护单任务状态，`start_*` 立即返回；worker 完成服务调用后推送状态，失败也进入可见状态。
- `paths.py` 统一源码和 `sys._MEIPASS` 路径，`single_instance.py` 阻止重复实例。
- 验收：桥接取消、非法路径、重复点击、推送失败和业务异常均不会导致应用退出或静默无响应。

### 阶段 6：把原型接到真实数据

涉及文件：`src/dna_retrieval_engine/resources/web/` 全部生产前端文件，重点包括 `bridge-client.js` 和 `state-store.js`。

- 从 `prd/` 复制生产副本，先接应用状态和生成流程，再接索引和搜索，最后接三个次级页面。
- 加入 `pywebviewready` 监听、超时/退避 Bridge 探测、统一 `callDesktopApi`、单飞轮询、页面隐藏/卸载清理。
- 下载并本地化 Lucide 固定版本；检查断网模式。
- 用 DOM API 渲染后端数据，删除所有硬编码样例计数和占位 toast。
- 验收：桥接尚未就绪、任务运行、完成、取消和失败均有可见界面；四个页面的同一 Read、坐标和汇总数字完全一致。

### 阶段 7：报告和展示细节

涉及文件：`report_service.py`、`result_export.py`、`resources/web/pages/report.html`、`docs/demo-script.md`。

- 导出 JSON/CSV，完成可打印 CSS。
- 补齐空状态、加载、成功、取消、校验失败、无命中、多命中等状态。
- 写固定 3-5 分钟演示脚本和备用随机种子。
- 验收：不看源代码也能从界面解释一次 Read 的完整检索路径。

### 阶段 8：测试与质量门禁

涉及文件：`tests/`、`scripts/check.ps1`。

- 单元测试覆盖数据结构和边界，集成测试覆盖“生成 -> 落盘 -> 读回 -> 建索引 -> 搜索 -> 导出”。
- 增加性质检查：阈值从 0 到 3 增加时，命中集合不能减少。
- 增加桌面契约检查：本地 HTTP Server、API 绑定窗口、`pywebviewready`、单飞轮询、`start_*` 快速返回、页面隐藏/卸载后的计时器清理。
- 使用无浏览器的 JS 状态测试和 API 模拟检查主流程；真实 EXE 只保留一次打包后烟测。
- 验收：检查脚本一次运行完成 lint、测试和覆盖率门槛。

### 阶段 9：单 EXE 打包与客户机验收

涉及文件：`packaging/DNA_Retrieval_Engine.spec`、`scripts/build.ps1`、`docs/release-checklist.md`。

- 先构建 `onedir`，检查缺失模块、静态资源和 WebView2；通过后再构建 `onefile`。
- spec 以根目录 `app.py` 为入口，`datas` 将 `resources/web/` 收集到 `dna_retrieval_engine/resources/web`，并把根目录 `icon.ico` 作为 EXE 图标引用；设置 `console=False`、固定产品名并排除未使用的 Qt/CEF 后端。
- 不启用 UPX，减少杀毒软件误报和排障变量。
- 最终测试中文路径、空格路径、只读目录、取消对话框、断网、重复启动/单实例、推送中断、轮询恢复和异常退出。
- 在未安装 Python 的干净 Windows 10/11 x64 虚拟机双击测试；记录 WebView2 版本和 EXE SHA-256。

## 11. 单文件 EXE 的具体策略

### 11.1 稳定入口与资源路径

PyInstaller `onefile` 启动时会把依赖和静态文件解压到临时 `_MEIxxxxxx` 目录。`paths.py` 必须成为唯一资源入口：

```python
from pathlib import Path
import sys


def package_root() -> Path:
    if getattr(sys, "frozen", False):
        return Path(sys._MEIPASS) / "dna_retrieval_engine"
    return Path(__file__).resolve().parent


def resource_path(*parts: str) -> Path:
    return package_root().joinpath(*parts)
```

源码和 EXE 都用 `resource_path("resources", "web", "index.html").resolve()` 的绝对本地路径加载页面，并由 pywebview 内置 HTTP Server 提供 `http://localhost` 来源。禁止通过当前工作目录查找 `prd`、HTML、脚本或图标，因为用户可能从资源管理器、快捷方式、命令行或其他程序启动 EXE。

根目录 `app.py` 保持稳定且轻量，只调用包内 `main()`；spec 始终分析该入口。包内 HTML/CSS/JS 是只读资源，不能把生成数据写到 `_MEIPASS` 或资源旁边。用户数据只写用户选择目录；日志和偏好若需要持久化，写 `%LOCALAPPDATA%\NEXUS_DNA_Retrieval_Engine`。

### 11.2 spec 的固定职责

`packaging/DNA_Retrieval_Engine.spec` 至少明确：

```text
entry script: app.py
datas:
  src/dna_retrieval_engine/resources/web
    -> dna_retrieval_engine/resources/web
exe icon: icon.ico
console: False
upx: False
exclude: 未使用的 Qt / CEF 后端
```

根目录现有 `icon.ico` 仅在 spec 中引用，构建过程不得修改源文件。pywebview、pythonnet 和 WebView2Loader 由 PyInstaller hooks/依赖分析收集；先检查 `onedir` 中的真实收集结果，不手工猜测 DLL 清单。

### 11.3 构建顺序与生命周期

1. 在干净 Python 3.13.15 x64 虚拟环境安装 `requirements.lock`。
2. 运行 ruff、pytest 和覆盖率门槛。
3. `--clean --noconfirm` 构建 `onedir`，运行完整生成/检索流程。
4. 检查本地 HTTP 页面、Bridge 遮罩消失、原生对话框、任务立即反馈、单飞轮询和页面切换后的恢复。
5. 通过后再构建 `onefile`，不要跳过 onedir 排障阶段。
6. 在未安装 Python 的干净 Windows 10/11 x64 虚拟机运行最终 EXE。

单文件启动比 `onedir` 稍慢，这是临时解压的正常代价，不能用假算法进度掩盖启动异常。不要请求管理员权限运行单文件 EXE。Windows EXE 必须在 Windows x64 环境构建，PyInstaller 不是跨平台编译器。

通过 Windows 命名互斥量限制单实例。第二次双击时显示“应用已在运行”并退出新进程，避免两个任务控制器并发写同一目录。窗口关闭时设置任务取消标志并停止接受新任务；后台线程不持有或调用 `evaluate_js`。

当前应用运行时不启动 PowerShell、CMD 或其他控制台子进程，因此无需添加子进程逻辑。以后若引入外部程序，必须使用参数列表、`check=True`、捕获标准错误，并在 Windows 设置 `subprocess.CREATE_NO_WINDOW`；`console=False` 只隐藏主 EXE 控制台，不能隐藏子进程窗口。

### 11.4 WebView2 交付边界

生产启动建议强制 `gui='edgechromium'`，不要静默退回已经过时的 MSHTML；否则原型中的现代 CSS 可能严重错位。若 WebView2 不存在，使用 Windows 原生 MessageBox 给出“请安装 Microsoft Edge WebView2 Runtime”的明确提示。课程展示电脑应在答辩前离线验证一次。

“只发一个 EXE”在目标机器已有 WebView2 时可以满足。若客户要求一台完全没有 WebView2、没有网络的旧 Windows 10 机器也必须仅靠一个 EXE 运行，则需要把 Fixed Version Runtime 一并打包，体积会增加 250 MB 以上。这是另一种交付规格，应在客户明确要求后单独评估，不建议作为默认课程交付。

## 12. 测试重点与常见失败点

### 12.1 必测用例

- 桶数组为空、单节点、多节点、强制哈希冲突。
- 同一 K-mer 在参考序列首位、中间和末尾重复出现。
- `N=K` 时恰好一个窗口，`N<K` 时明确拒绝。
- 候选靠近参考末尾且 Read 越界。
- 0、1、2、3 个错配；阈值分别为 0-3。
- 第 `K-1` 位错配导致无真实候选，第 K 位之后错配可正常容错。
- 一条 Read 多个合法位置，结果不能被后一个覆盖。
- FASTA 小写、空行、非法字符、多序列；FASTQ 缺行、长度不一致、重复 ID。
- 完整数据集文件夹使用规范文件名时可直接解析；`ground_truth.json` 缺失时仍可加载。
- 文件夹中缺少 FASTA/FASTQ、同类型有多个候选、误选多个数据集的父目录时返回对应错误且不破坏旧数据集。
- 分别选择的参考和 Reads 位于不同目录时可加载；只选择一个文件或取消另一个时不启动任务。
- 用户选择中文/空格路径、没有写权限路径、取消选择和已有同名目录。
- 同一种子生成内容一致，不同种子大概率不同。
- 断网启动时所有图标和页面完整。

### 12.2 原型当前已知的接入风险

1. 原型所有核心数字和 Reads 都是固定样例，必须整体替换，不能只让“运行”按钮调用 Python 而页面仍显示旧数据。
2. `app.js` 当前只构造 6 条 30 bp Read，生产数据是 50-150 bp，序列容器必须处理更长内容。
3. `pages.js` 会重新虚构 50 条 Read，与主工作台数据源分裂。
4. 四个 HTML 依赖外部 unpkg CDN，不符合离线和单 EXE 要求。
5. 原型以 47 条命中为展示目标；当前生成器按种子随机分配 0-3 错配并保证四类均出现，因此阈值 2 的命中数随种子变化，不能硬编码为固定数量。
6. 课程文本示例阈值是 2，用户的新生成需求是 0-3。默认检索阈值仍为 2，3 错配 Read 专门用于展示拒绝、近似对齐和剪枝，调到 3 后再展示全部找回。

### 12.3 pywebview 与打包回归点

1. `desktop.main` 使用 `paths.resource_path(...).resolve()` 得到绝对本地 HTML 路径，由 pywebview 内置 HTTP Server 提供页面。
2. `DesktopApi` 保存并只使用 `create_window` 返回的窗口对象。
3. 前端监听 `pywebviewready`，使用单一串行轮询、超时、退避和页面生命周期清理，不缓存 rejected 就绪 Promise。
4. 运行按钮的监听器确实安装；全局助手和元素 `id/name` 不发生 WebView2 命名冲突。
5. `start_*` 在启动 worker 后立即返回，重复任务收到明确业务错误；窗口 closing 后不再接受新任务。
6. 后台任务只更新线程安全状态，`get_state` 串行返回最新状态并附带 capability 权限矩阵；可选事件推送失败不影响任务。
7. 后端异常、取消和路径校验失败都能进入可见界面，无控制台时仍可诊断。
8. `paths.py` 在源码和模拟 `sys._MEIPASS` 下都指向相同相对资源结构。
9. spec 收集全部 HTML/CSS/JS/vendor 资源，`console=False`、`upx=False` 和 `icon.ico` 生效。
10. 打包后启动 EXE，等待 Bridge 就绪，选择中文路径，生成数据，建立索引，运行检索并导出一次结果；不能只验证窗口能打开。

## 13. 最终交付物清单

- `dist/NEXUS_DNA_Retrieval_Engine.exe`
- 源代码及 `requirements.lock`
- 手写数据结构和算法测试报告
- `docs/architecture.md` 与复杂度说明
- `docs/data-format.md`
- `docs/demo-script.md`
- `docs/release-checklist.md`
- 打包后真实流程冒烟记录
- EXE 的 SHA-256、构建日期、Python/pywebview/PyInstaller 版本记录

客户只需要 EXE；其余内容用于课程提交、复现和维护。生成的数据、运行结果和报告均由客户自行选择目录保存，不随 EXE 一起发放。

## 14. 资料与实践依据

- `D:\工具开发\pywebview桌面应用与EXE打包实践.md`：采用其窗口显式绑定、Bridge 就绪探测、后台任务、推送与轮询、`sys._MEIPASS`、单实例和打包后冒烟方法；子进程规则作为未来扩展保留。

- [pywebview JavaScript-Python bridge](https://pywebview.flowrl.com/guide/interdomain.html)：`js_api`、Promise、`pywebviewready` 和线程安全说明。
- [pywebview API](https://pywebview.flowrl.com/api/)：窗口创建、文件夹/文件对话框、WebView2 Runtime 路径和图标参数。
- [pywebview Freezing](https://pywebview.flowrl.com/guide/freezing.html)：Windows/Linux 使用 PyInstaller、静态文件和 onefile 打包方式。
- [pywebview Web engine](https://pywebview.flowrl.com/guide/web_engine.html)：Windows 渲染器顺序与 WebView2 前提。
- [PyInstaller operating mode](https://pyinstaller.org/en/stable/operating-mode.html)：onedir/onefile、临时解压、目标平台构建和无 Python 运行环境。
- [PyInstaller runtime information](https://pyinstaller.org/en/stable/runtime-information.html)：打包后的 `__file__`/bundle 路径行为；本项目由 `paths.py` 集中封装 `sys._MEIPASS`。
- [Microsoft WebView2 distribution](https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/distribution)：Evergreen/Fixed Version、Windows 10/11 覆盖与部署选择。
- [NCBI FASTA format](https://www.ncbi.nlm.nih.gov/genbank/fastaformat/)：FASTA 定义行、序列 ID 和序列记录规则。
- [NCBI SRA submission formats](https://www.ncbi.nlm.nih.gov/sra/docs/submitformats/#fastq-files)：FASTQ 提交格式与质量字符串要求。
- [pywebview on PyPI](https://pypi.org/project/pywebview/) 与 [PyInstaller on PyPI](https://pypi.org/project/pyinstaller/)：调研日版本和 Python 版本支持信息。
